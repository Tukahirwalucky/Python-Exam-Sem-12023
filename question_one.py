#write a python function named "calculate_area"  that takes the radiusof a circle and return the  area of a parameter

def calulate_area():
    x=3.14
    r=5
    result=r*r^2
    print(type[float(result)])

calculate_area()    